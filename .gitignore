node_modules
dist
coverage
reports
stryker-tmp
.stryker-tmp

# Keep environment variables out of version control
.env

/generated/prisma

# Default STORAGE_DRIVER=local write target (see .env.example) — runtime
# uploads, not source.
/storage
