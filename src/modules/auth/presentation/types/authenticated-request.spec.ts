import * as authenticatedRequest from './authenticated-request';

describe('authenticated-request', () => {
  it('is a type-only module with no runtime exports', () => {
    expect(authenticatedRequest).toBeDefined();
  });
});
