import * as authSessionResult from './auth-session.result';

describe('auth-session.result', () => {
  it('is a type-only module with no runtime exports', () => {
    expect(authSessionResult).toBeDefined();
  });
});
